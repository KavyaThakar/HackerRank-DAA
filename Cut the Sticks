 math
import os
import random
import re
import sys

def cutTheSticks(arr):
    result = []
    arr.sort()
    n = len(arr)
    i = 0
    while i < n:
        result.append(n - i)
        cut = arr[i]
        while i < n and arr[i] == cut:
            i += 1
    return result

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    res = cutTheSticks(arr)
    print(*res, sep='\n')
